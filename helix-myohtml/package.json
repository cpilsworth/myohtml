{
  "name": "helix-myohtml",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test",
    "check": "node --test"
  },
  "devDependencies": {
    "@mermaid-js/mermaid-cli": "^11.12.0",
    "wrangler": "^4.0.0"
  }
}
